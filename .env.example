# Copy to .env and fill in. docker-compose reads this automatically.

# Which LLM the agent uses: claude (default) | openai | mistral | mock
# Use `mock` to run the whole stack with NO API key and no cost — the agents
# still call the real MCP tools and return live data.
LLM_PROVIDER=claude

# Optional: override the model id for the chosen provider.
# claude  -> claude-opus-4-8 (default)
# openai  -> gpt-4o (default)
# mistral -> mistral-large-latest (default)
LLM_MODEL=

# Provide the key matching LLM_PROVIDER (leave the others blank, none for mock).
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
MISTRAL_API_KEY=

# Host ports (containers always use 8000/8001/8002 internally).
# Override only if those ports are already taken on your machine.
API_PORT=8000
MCP_PORT=8001
WEB_PORT=8002
