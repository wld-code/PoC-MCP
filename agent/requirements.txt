# Core: MCP client
mcp==1.27.2

# Web UI agent (web.py)
fastapi==0.115.6
uvicorn[standard]==0.34.0

# LLM providers — install the one(s) you use. All listed so the image is
# provider-agnostic out of the box; trim for a slimmer build.
anthropic==0.69.0
openai==1.59.6
mistralai==1.2.5
